✅ 1. Install Node.js
Node.js allows you to run JavaScript outside the browser.
It comes with npm (Node Package Manager), which helps you install packages.

▶️ Steps:
Go to https://nodejs.org

Download the LTS version (recommended for most users).

Run the installer and follow the prompts.

After installation, verify:


node -v     # should show version, e.g., v18.x.x
npm -v      # should show version, e.g., 9.x.x
✅ 2. Install Git
Git is a version control system used to track code changes.

▶️ Steps:
Go to https://git-scm.com/downloads

Download and install it for your operating system.

After installation, verify:

git --version  # should show version, e.g., git version 2.x.x
✅ 3. Git Setup (First-time configuration)
You need to set your identity so commits are linked to your name/email.

▶️ Run the following:

git config --global user.name "Your Full Name"
git config --global user.email "you@example.com"
Optional:

git config --global init.defaultBranch main
You can check your setup using:


git config --global --list
✅ 4. Install Postman
Postman is a tool for testing APIs (HTTP requests).

▶️ Steps:
Go to https://www.postman.com/downloads

Download and install the app for your OS.

Sign in (or create a free account) to access your workspace.

You can now send requests like GET, POST, PUT, etc.

✅ 5. Read a Little About JavaScript
You don’t need to master JS before starting, but you should be familiar with:

🧠 Basics to Know:
Variables: let, const

Functions: function, arrow functions

Data types: strings, numbers, booleans, arrays, objects

Loops: for, while, forEach

Conditionals: if, else, switch

Basic DOM and events (optional if you're backend-focused)

📖 Resources:
MDN JavaScript Guide

JavaScript.info

W3Schools JavaScript

